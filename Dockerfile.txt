# Dockerfile para Forge3D Cotizador
FROM nginx:alpine

# Información del mantenedor
LABEL maintainer="GoldTech MX <soporte@goldtech.mx>"
LABEL description="Cotizador Forge3D - Calculadora profesional de precios para impresión 3D"
LABEL version="2.1.0"

# Instalar dependencias adicionales
RUN apk add --no-cache \
    curl \
    bash \
    && rm -rf /var/cache/apk/*

# Crear directorio de trabajo
WORKDIR /usr/share/nginx/html

# Copiar archivos de la aplicación
COPY . .

# Copiar configuración personalizada de nginx
COPY docker/nginx.conf /etc/nginx/nginx.conf
COPY docker/default.conf /etc/nginx/conf.d/default.conf

# Crear usuario no-root para seguridad
RUN addgroup -g 1001 forge3d && \
    adduser -D -s /bin/bash -u 1001 -G forge3d forge3d

# Establecer permisos correctos
RUN chown -R forge3d:forge3d /usr/share/nginx/html && \
    chmod -R 755 /usr/share/nginx/html

# Cambiar al usuario no-root
USER forge3d

# Exponer puerto
EXPOSE 8080

# Healthcheck
HEALTHCHECK --interval=30s --timeout=3s --start-period=5s --retries=3 \
    CMD curl -f http://localhost:8080/health || exit 1

# Comando por defecto
CMD ["nginx", "-g", "daemon off;"]